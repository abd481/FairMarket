# Base URL of the FairMarket FastAPI backend.
# For local development against the Python API running on port 8000:
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional: enable a small set of mock responses for frontend-only development.
# Set to "true" only when you cannot run the backend. Do not ship this.
# NEXT_PUBLIC_USE_MOCK=true